Carefully read, internalize, and strictly follow both `AGENTS.md` and `agent_rules.yaml` from the repository's root before doing anything else. Treat `AGENTS.md` as your project guidelines and `agent_rules.yaml` as your behavioral constraints. 